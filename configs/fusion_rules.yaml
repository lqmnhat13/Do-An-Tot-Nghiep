# Quy tắc phân vùng không gian, đồng bộ thời gian và FSM đánh giá rủi ro
spatial:
  left_ratio: 0.35   # 0% - 35%: Vùng bên trái
  center_ratio: 0.30 # 35% - 65%: Vùng trực diện phía trước
  right_ratio: 0.35  # 65% - 100%: Vùng bên phải

synchronizer:
  max_pair_skew_ms: 600      # Độ lệch thời gian tối đa cho phép giữa detection và depth (ms)
  max_detection_age_ms: 1000 # Tuổi tối đa của detection trước khi coi là stale (ms)
  max_depth_age_ms: 1200     # Tuổi tối đa của depth trước khi coi là stale (ms)
  max_alert_age_ms: 1500     # Tuổi tối đa của dữ liệu nguồn khi chuẩn bị phát cảnh báo (ms)

risk_fsm:
  # Ngưỡng relative depth (đã chuẩn hóa [0, 1], trong đó giá trị lớn hơn = gần hơn)
  depth_threshold_high: 0.60
  depth_threshold_medium: 0.40
  depth_threshold_low: 0.20

  # Hysteresis: số frame xác nhận để chuyển trạng thái (1 frame phản hồi tức thì cho người mù)
  confirmations_to_escalate: 1
  confirmations_to_deescalate: 3
  instant_high_risk_in_center: true # Kích hoạt nhanh ngay khi vật cản ở vùng giữa

  # Cooldown tránh lặp lại cảnh báo cùng một đối tượng (giây)
  track_alert_cooldown_sec: 2.5
  # Cho phép ghi đè cooldown nếu mức rủi ro tăng cấp (ví dụ từ MEDIUM lên HIGH)
  allow_escalation_override: true

labels_vi:
  person: "người"
  cat: "mèo"
  dog: "chó"
  chair: "ghế"
  couch: "ghế sofa"
  bed: "giường"
  dining table: "bàn ăn"
  tv: "ti vi"
  laptop: "máy tính xách tay"
  keyboard: "bàn phím"
  mouse: "chuột máy tính"
  cell phone: "điện thoại"
  bottle: "chai nước"
  cup: "cốc nước"
  bowl: "bát"
  book: "sách"
  clock: "đồng hồ"
  vase: "bình hoa"
  microwave: "lò vi sóng"
  oven: "lò nướng"
  toaster: "máy nướng bánh"
  sink: "bồn rửa"
  refrigerator: "tủ lạnh"
  toilet: "bồn cầu"

directions_vi:
  LEFT: "bên trái"
  CENTER: "phía trước"
  RIGHT: "bên phải"
